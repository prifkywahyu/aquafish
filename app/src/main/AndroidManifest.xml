<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mobile.aquafish">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity android:name=".SplashScreen"
            android:screenOrientation="fullSensor"
            android:theme="@style/CustomTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:theme="@style/ThemeActivity"
            android:screenOrientation="fullSensor">
        </activity>

        <activity android:name=".MainRegister"
            android:theme="@style/ThemeActivity"
            android:screenOrientation="fullSensor">
        </activity>

        <activity android:name=".FragmentMain"
            android:theme="@style/AppTheme"
            android:screenOrientation="fullSensor">
        </activity>

        <activity android:name=".ReportTemperature"
            android:theme="@style/AppTheme"
            android:screenOrientation="fullSensor">
        </activity>

        <activity android:name=".ReportTurbidity"
            android:theme="@style/AppTheme"
            android:screenOrientation="fullSensor">
        </activity>

        <activity android:name=".ReportWaterLevel"
            android:theme="@style/AppTheme"
            android:screenOrientation="fullSensor">
        </activity>
    </application>
</manifest>